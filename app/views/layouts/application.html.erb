<!DOCTYPE html>
<html>
  <head>
    <title>BankingApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body style="background-color: #f1f1f1;">
  <%= render partial: "shared/header" %>
   <!--<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #87CEEB;">  <div class="container-fluid">
    <%= link_to "Online Banking", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
   <% if user_signed_in? %>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <%= link_to "Home", root_path, class: "nav-link" %>
      <%= link_to "User Profile", user_path(current_user), class: "nav-link" %>
      <%= link_to "My Posts", posts_path, class: "nav-link" %>
      <%= link_to "Create New Posts", new_post_path, class: "nav-link" %>
    </div>
    <div class="navbar-nav ms-auto">
      <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-danger" %>
    </div> 
  </div>
<% end %>
 
</nav>
 </div> -->
<% if user_signed_in? %> 
  <p style="background-color: #f0f0f0; padding: 13px;"> <strong> Logged in as </strong> <%= current_user.email %>  <p> 
<% end %>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>


    <%= yield %>


  </body>
</html>
